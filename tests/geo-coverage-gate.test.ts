import { describe, expect, it } from 'vitest'
import { buildEngineCoverage, evaluateCoverageGate, type LedgerRow } from '../lib/geo/coverage'
const row = (engine: string, query_id: string, status: LedgerRow['status']): LedgerRow => ({ engine, query_id, query: query_id, sample_index: 1, status, attempts: 1, answer_length: 300, citations_count: 1, observed_at: '2026-01-01T00:00:00.000Z' })
describe('A1 coverage gate', () => { it('passes 18/18', () => { const l = ['claude','openai','perplexity'].flatMap(e => Array.from({length:6},(_,i)=>row(e,`Q${i+1}`,'ok_grounded'))); expect(evaluateCoverageGate(buildEngineCoverage(l,6,['claude','openai','perplexity'])).passed).toBe(true) }); it('fails Vertex-like 9/18', () => { const l = [...Array.from({length:6},(_,i)=>row('openai',`Q${i+1}`,'ok_grounded')),...Array.from({length:2},(_,i)=>row('claude',`Q${i+1}`,'ok_grounded')),...Array.from({length:1},(_,i)=>row('perplexity',`Q${i+1}`,'ok_grounded'))]; expect(evaluateCoverageGate(buildEngineCoverage(l,6,['claude','openai','perplexity'])).passed).toBe(false) }) })
